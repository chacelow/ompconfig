# References

Local source clones used as design references. Not vendored into runtime;
each project directory is `.gitignore`d.

- `pi-observational-memory/` — MIT, upstream:
  `git clone git@github.com:amosblomqvist/pi-observational-memory.git`
  Borrowed: StatusController pattern (gauges, spinner, worker widget),
  timeline glyph vocabulary, ToastCoalescer, branch-aware ledger fold.
